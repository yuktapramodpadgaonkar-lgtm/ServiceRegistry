apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  labels:
    app: user-service
spec:
  replicas: 2  # Run 2 instances for demonstration
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: service-registry:latest
        imagePullPolicy: Never
        command: ["python", "microservice_instance.py"]
        args: ["user-service", "8001"]
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: REGISTRY_URL
          value: "http://service-registry:5001"
        - name: PYTHONUNBUFFERED
          value: "1"
        ports:
        - containerPort: 8001
          name: http
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: user-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  labels:
    app: payment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
      - name: payment-service
        image: service-registry:latest
        imagePullPolicy: Never
        command: ["python", "microservice_instance.py"]
        args: ["payment-service", "8002"]
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: REGISTRY_URL
          value: "http://service-registry:5001"
        - name: PYTHONUNBUFFERED
          value: "1"
        ports:
        - containerPort: 8002
          name: http
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: payment-service

# Made with Bob
